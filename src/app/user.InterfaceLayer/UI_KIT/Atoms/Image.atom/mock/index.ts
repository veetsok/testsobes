import ImageEnum from "../enum";

const ImagesMock = {
	children: "Демо",
	type: ImageEnum.enum_srcImage,
};

export default ImagesMock;

export const IImagePropsDefault = {
  children: ImagesMock.children,
  type: ImagesMock.type,
};
